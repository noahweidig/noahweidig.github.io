/** Shared between the Hero's <Image> and the preload link the homepage puts in
    <head>: the two have to describe the same candidate or the browser fetches
    the portrait twice. */
export const PORTRAIT_WIDTHS = [400, 700];
export const PORTRAIT_SIZES = '(min-width: 1024px) 380px, (min-width: 640px) 224px, 168px';
