# blog-write-oauth-worker

Exchanges a GitHub OAuth `code` for an access token, server-side, so the
client secret never reaches the browser. Used by `/blog/write`.

See the repo root README or the setup instructions given alongside this
change for how to create the GitHub OAuth App and deploy this Worker.
